import { Template, TemplateOutputFile } from "../../index";
import { ResolvedWunderGraphConfig } from "../../../configure";
export declare const formatTypeScript: (input: string) => string;
export declare class TypeScriptInputModels implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare class TypeScriptInternalInputModels implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare class TypeScriptInjectedInputModels implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare class TypeScriptResponseModels implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare class TypeScriptResponseDataModels implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare const loadFile: (file: any) => string;
