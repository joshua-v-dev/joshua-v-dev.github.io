import { Template, TemplateOutputFile } from "../../index";
import { ResolvedApplication, ResolvedWunderGraphConfig } from "../../../configure";
import { OperationType } from "@wundergraph/protobuf/dist/wundernode_config";
export declare class TypeScriptClient implements Template {
    constructor(reactNative?: boolean);
    private readonly reactNative;
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare const operations: (application: ResolvedApplication, operationType: OperationType, includeInternal: boolean) => {
    operationName: string;
    path: string;
    hasInput: boolean | undefined;
    hasInternalInput: boolean | undefined;
}[];
export declare const liveQueries: (application: ResolvedApplication, includeInternal: boolean) => {
    operationName: string;
    path: string;
    hasInput: boolean | undefined;
    hasInternalInput: boolean | undefined;
}[];
export declare const modelImports: (application: ResolvedApplication, includeInternal: boolean) => string;
