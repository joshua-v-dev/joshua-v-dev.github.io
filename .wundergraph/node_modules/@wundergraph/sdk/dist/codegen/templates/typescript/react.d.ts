import { Template, TemplateOutputFile } from "../../index";
import { ResolvedWunderGraphConfig } from "../../../configure";
import { GraphQLOperation } from "../../../graphql/operations";
export declare class TypescriptReactProvider implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare class TypescriptReactNativeProvider implements Template {
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare class TypescriptReactHooks implements Template {
    constructor(reactNative?: boolean);
    private readonly reactNative;
    generate(config: ResolvedWunderGraphConfig): Promise<TemplateOutputFile[]>;
}
export declare const isNotInternal: (op: GraphQLOperation) => boolean;
export declare const hasInput: (op: GraphQLOperation) => boolean;
export declare const hasInternalInput: (op: GraphQLOperation) => boolean;
