import { AuthProvider } from "@wundergraph/protobuf/dist/wundernode_config";
import { InputVariable } from "./index";
export interface AuthenticationProvider {
    resolve: () => AuthProvider;
}
export interface GithubAuthProviderConfig {
    id: string;
    clientId: InputVariable;
    clientSecret: InputVariable;
}
export declare class GithubAuthProvider implements AuthenticationProvider {
    private readonly config;
    constructor(config: GithubAuthProviderConfig);
    resolve(): AuthProvider;
}
export interface OpenIDConnectAuthProviderConfig {
    id: string;
    issuer: InputVariable;
    clientId: InputVariable;
    clientSecret: InputVariable;
}
export declare class OpenIDConnectAuthProvider implements AuthenticationProvider {
    private readonly config;
    constructor(config: OpenIDConnectAuthProviderConfig);
    resolve(): AuthProvider;
}
export interface GoogleAuthProviderConfig {
    id: string;
    clientId: InputVariable;
    clientSecret: InputVariable;
}
export declare const authProviders: {
    github: (config: GithubAuthProviderConfig) => GithubAuthProvider;
    demo: () => GithubAuthProvider;
    openIdConnect: (config: OpenIDConnectAuthProviderConfig) => OpenIDConnectAuthProvider;
    google: (config: GoogleAuthProviderConfig) => OpenIDConnectAuthProvider;
};
